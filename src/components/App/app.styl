@import '~/styles/vars'

.userInputForm
  text-align: right
  height 3rem

.chatBox
  border 1px solid black
  height 350px
  display flex
  flex-direction: column
  justify-content: space-between
  overflow hidden
  text-align: left

  &-box
    height 100%
    overflow-y scroll
  &-input 
    width 100%
    height 5em
h1 
  font-size 3em
  text-align center
  margin-bottom 1em

p
  padding: 0.5em